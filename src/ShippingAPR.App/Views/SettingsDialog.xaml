<Window x:Class="ShippingAPR.App.Views.SettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:enums="clr-namespace:ShippingAPR.Core.Enums;assembly=ShippingAPR.Core"
        Title="Settings"
        Width="520" Height="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Border Margin="16">
        <Border.Effect>
            <DropShadowEffect BlurRadius="32" ShadowDepth="0" Opacity="0.35"
                              Color="{DynamicResource AccentColor}"/>
        </Border.Effect>

        <Border Background="{DynamicResource PrimaryBackgroundBrush}"
                CornerRadius="16"
                BorderThickness="1"
                BorderBrush="{DynamicResource GlassEdgeBrush}"
                Padding="32">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <StackPanel Grid.Row="0" Margin="0,0,0,20">
                    <TextBlock Text="Settings"
                               FontSize="22" FontWeight="Bold"
                               Foreground="{DynamicResource PrimaryTextBrush}"/>
                    <TextBlock Text="Configure AIS data providers and API keys"
                               FontSize="13"
                               Foreground="{DynamicResource SecondaryTextBrush}"
                               Margin="0,4,0,0"/>
                </StackPanel>

                <!-- Settings Content -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel>

                        <!-- Provider Selection -->
                        <TextBlock Text="DATA PROVIDER" FontSize="11" FontWeight="SemiBold"
                                   Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,8"/>

                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="Active Provider" FontSize="13"
                                       Foreground="{DynamicResource PrimaryTextBrush}"
                                       VerticalAlignment="Center" Margin="0,0,0,6"/>
                            <ComboBox Grid.Column="1"
                                      ItemsSource="{Binding AvailableProviders}"
                                      SelectedItem="{Binding ActiveProvider}"
                                      Margin="0,0,0,6"/>

                            <TextBlock Grid.Row="1" Text="Fallback Provider" FontSize="13"
                                       Foreground="{DynamicResource PrimaryTextBrush}"
                                       VerticalAlignment="Center"/>
                            <ComboBox Grid.Row="1" Grid.Column="1"
                                      ItemsSource="{Binding FallbackOptions}"
                                      SelectedItem="{Binding FallbackProvider}"/>
                        </Grid>

                        <!-- Poll interval -->
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                            <TextBlock Text="Poll Interval (seconds):" FontSize="13"
                                       Foreground="{DynamicResource PrimaryTextBrush}"
                                       VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding PollIntervalSeconds, UpdateSourceTrigger=PropertyChanged}"
                                     Width="60"
                                     Background="{DynamicResource GlassFillBrush}"
                                     Foreground="{DynamicResource PrimaryTextBrush}"
                                     BorderBrush="{DynamicResource GlassBorderBrush}"
                                     Padding="8,6"/>
                        </StackPanel>

                        <!-- API Keys -->
                        <TextBlock Text="API KEYS" FontSize="11" FontWeight="SemiBold"
                                   Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,8"/>

                        <!-- AisStream -->
                        <TextBlock Text="AisStream (free WebSocket streaming)" FontSize="13"
                                   Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,4"/>
                        <Border Background="{DynamicResource GlassFillBrush}"
                                BorderBrush="{DynamicResource GlassBorderBrush}"
                                BorderThickness="1" CornerRadius="8" Margin="0,0,0,12">
                            <TextBox Text="{Binding AisStreamApiKey, UpdateSourceTrigger=PropertyChanged}"
                                     Background="Transparent"
                                     Foreground="{DynamicResource PrimaryTextBrush}"
                                     BorderThickness="0" Padding="10,8"
                                     FontFamily="Consolas" FontSize="13"/>
                        </Border>

                        <!-- Datalastic -->
                        <TextBlock Text="Datalastic (REST API)" FontSize="13"
                                   Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,4"/>
                        <Border Background="{DynamicResource GlassFillBrush}"
                                BorderBrush="{DynamicResource GlassBorderBrush}"
                                BorderThickness="1" CornerRadius="8" Margin="0,0,0,12">
                            <TextBox Text="{Binding DatalasticApiKey, UpdateSourceTrigger=PropertyChanged}"
                                     Background="Transparent"
                                     Foreground="{DynamicResource PrimaryTextBrush}"
                                     BorderThickness="0" Padding="10,8"
                                     FontFamily="Consolas" FontSize="13"/>
                        </Border>

                        <!-- DataDocked -->
                        <TextBlock Text="Data Docked (REST API + Satellite AIS)" FontSize="13"
                                   Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,4"/>
                        <Border Background="{DynamicResource GlassFillBrush}"
                                BorderBrush="{DynamicResource GlassBorderBrush}"
                                BorderThickness="1" CornerRadius="8" Margin="0,0,0,12">
                            <TextBox Text="{Binding DataDockedApiKey, UpdateSourceTrigger=PropertyChanged}"
                                     Background="Transparent"
                                     Foreground="{DynamicResource PrimaryTextBrush}"
                                     BorderThickness="0" Padding="10,8"
                                     FontFamily="Consolas" FontSize="13"/>
                        </Border>

                        <!-- VesselFinder -->
                        <TextBlock Text="VesselFinder (vessel enrichment)" FontSize="13"
                                   Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,4"/>
                        <Border Background="{DynamicResource GlassFillBrush}"
                                BorderBrush="{DynamicResource GlassBorderBrush}"
                                BorderThickness="1" CornerRadius="8" Margin="0,0,0,12">
                            <TextBox Text="{Binding VesselFinderApiKey, UpdateSourceTrigger=PropertyChanged}"
                                     Background="Transparent"
                                     Foreground="{DynamicResource PrimaryTextBrush}"
                                     BorderThickness="0" Padding="10,8"
                                     FontFamily="Consolas" FontSize="13"/>
                        </Border>

                    </StackPanel>
                </ScrollViewer>

                <!-- Buttons -->
                <StackPanel Grid.Row="2" Orientation="Horizontal"
                            HorizontalAlignment="Right" Margin="0,20,0,0">
                    <Button Content="Cancel"
                            Click="CancelClick"
                            Style="{DynamicResource GhostButtonStyle}"
                            Padding="20,10" FontSize="13"
                            Margin="0,0,10,0"/>
                    <Button Content="Save &amp; Restart"
                            Click="SaveClick"
                            Style="{DynamicResource AccentButtonStyle}"
                            Padding="24,10" FontSize="13"/>
                </StackPanel>
            </Grid>
        </Border>
    </Border>
</Window>
