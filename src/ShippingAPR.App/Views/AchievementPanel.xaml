<UserControl x:Class="ShippingAPR.App.Views.AchievementPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:res="clr-namespace:ShippingAPR.App.Resources">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="12">
        <StackPanel Margin="8">

            <!-- ═══ Header ═══ -->
            <TextBlock Text="{x:Static res:Strings.ShipSpotter}" FontSize="16" FontWeight="Bold"
                       Foreground="{DynamicResource PrimaryTextBrush}"
                       Margin="0,0,0,4"/>
            <TextBlock Text="{x:Static res:Strings.TrackDiscoveries}" FontSize="11"
                       Foreground="{DynamicResource SecondaryTextBrush}"
                       Margin="0,0,0,12"/>

            <!-- ═══ Progress Overview ═══ -->
            <Border Style="{DynamicResource CardStyle}" Margin="0,0,0,10">
                <StackPanel>
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{x:Static res:Strings.Achievements}" Style="{DynamicResource SubTextStyle}" FontWeight="Bold"/>
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <TextBlock Text="{Binding TotalUnlocked}" FontWeight="Bold"
                                       Foreground="{DynamicResource AccentBrush}" FontSize="16"/>
                            <TextBlock Text="/" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="16"/>
                            <TextBlock Text="{Binding TotalAchievements}"
                                       Foreground="{DynamicResource SecondaryTextBrush}" FontSize="16"/>
                        </StackPanel>
                    </Grid>
                    <!-- Progress bar -->
                    <Grid Height="6" Margin="0,0,0,4">
                        <Border Background="{DynamicResource GlassBorderBrush}" CornerRadius="3"/>
                    </Grid>
                    <TextBlock Text="{Binding ProgressPercent, StringFormat='{}{0:F0}% complete'}"
                               Foreground="{DynamicResource SecondaryTextBrush}"
                               FontSize="10" HorizontalAlignment="Right"/>
                </StackPanel>
            </Border>

            <!-- ═══ Spotter Stats ═══ -->
            <Border Style="{DynamicResource CardStyle}" Margin="0,0,0,10">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <Border Width="3" Height="14" CornerRadius="2"
                                Background="{DynamicResource AccentBrush}" Margin="0,0,8,0"/>
                        <TextBlock Text="{x:Static res:Strings.YourStats}" Style="{DynamicResource SubTextStyle}"
                                   FontWeight="Bold" FontSize="11"/>
                    </StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                            <RowDefinition/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="{x:Static res:Strings.VesselsSpotted}"
                                   Style="{DynamicResource SubTextStyle}"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding UniqueVessels}"
                                   Foreground="{DynamicResource AccentBrush}" FontWeight="Bold" FontSize="14"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="{x:Static res:Strings.TypesDiscovered}" Margin="0,4,0,0"
                                   Style="{DynamicResource SubTextStyle}"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding TypesDiscovered}" Margin="0,4,0,0"
                                   Style="{DynamicResource ValueTextStyle}" FontWeight="Bold"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="{x:Static res:Strings.CountriesTracked}" Margin="0,4,0,0"
                                   Style="{DynamicResource SubTextStyle}"/>
                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding CountriesTracked}" Margin="0,4,0,0"
                                   Style="{DynamicResource ValueTextStyle}" FontWeight="Bold"/>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="{x:Static res:Strings.FastestSpotted}" Margin="0,4,0,0"
                                   Style="{DynamicResource SubTextStyle}"/>
                        <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding FastestSpeedText}" Margin="0,4,0,0"
                                   Foreground="#FFD700" FontWeight="Bold"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- ═══ Achievement List ═══ -->
            <ItemsControl ItemsSource="{Binding Achievements}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,0,0,6" Padding="10,8"
                                Background="{DynamicResource GlassFillBrush}"
                                BorderBrush="{DynamicResource GlassBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="36"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Icon -->
                                <TextBlock Grid.Column="0" Text="{Binding Icon}" FontSize="20"
                                           VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Opacity" Value="0.3"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsUnlocked}" Value="True">
                                                    <Setter Property="Opacity" Value="1.0"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>

                                <!-- Name + Description -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="12">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsUnlocked}" Value="True">
                                                        <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <TextBlock Text="{Binding Description}"
                                               Foreground="{DynamicResource SecondaryTextBrush}"
                                               FontSize="10" Opacity="0.7"/>
                                </StackPanel>

                                <!-- Progress -->
                                <TextBlock Grid.Column="2" Text="{Binding ProgressText}"
                                           VerticalAlignment="Center" FontSize="11"
                                           Margin="8,0,0,0">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsUnlocked}" Value="True">
                                                    <Setter Property="Foreground" Value="#4CAF50"/>
                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </StackPanel>
    </ScrollViewer>
</UserControl>
