<UserControl x:Class="ShippingAPR.App.Views.EncounterJournalPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="12,8,12,4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Ship Journal" FontSize="16" FontWeight="Bold"
                           Foreground="{DynamicResource PrimaryTextBrush}"/>
                <Button Grid.Column="1" Content="Export" Command="{Binding ExportJournalCommand}"
                        Style="{DynamicResource GhostButtonStyle}" FontSize="10" Padding="8,2"/>
            </Grid>
            <TextBlock Text="Your personal ship-spotter's logbook"
                       Style="{DynamicResource SubTextStyle}" FontSize="11" Opacity="0.7"/>
        </StackPanel>

        <!-- Stats bar -->
        <Border Grid.Row="1" Style="{DynamicResource CardStyle}" Margin="12,4,12,4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                    <TextBlock Text="{Binding TotalEncounters}" FontSize="18" FontWeight="Bold"
                               Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Center"/>
                    <TextBlock Text="Spotted" Style="{DynamicResource SubTextStyle}"
                               FontSize="9" HorizontalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                    <TextBlock Text="{Binding UniqueCountries}" FontSize="18" FontWeight="Bold"
                               Foreground="{DynamicResource PrimaryTextBrush}" HorizontalAlignment="Center"/>
                    <TextBlock Text="Countries" Style="{DynamicResource SubTextStyle}"
                               FontSize="9" HorizontalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                    <TextBlock Text="{Binding UniqueTypes}" FontSize="18" FontWeight="Bold"
                               Foreground="{DynamicResource PrimaryTextBrush}" HorizontalAlignment="Center"/>
                    <TextBlock Text="Types" Style="{DynamicResource SubTextStyle}"
                               FontSize="9" HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Search -->
        <Border Grid.Row="2" Margin="12,4,12,4" Background="{DynamicResource GlassFillBrush}"
                CornerRadius="8" Padding="8,4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="&#x1F50D;" FontSize="12" VerticalAlignment="Center" Margin="0,0,6,0"
                           Foreground="{DynamicResource SecondaryTextBrush}"/>
                <TextBox Grid.Column="1" Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                         Background="Transparent" BorderThickness="0"
                         Foreground="{DynamicResource PrimaryTextBrush}"
                         FontSize="12" VerticalAlignment="Center"/>
                <Button Grid.Column="2" Content="&#x2715;" Command="{Binding ClearSearchCommand}"
                        Style="{DynamicResource GhostButtonStyle}" FontSize="10" Padding="4"/>
            </Grid>
        </Border>

        <!-- Encounter list -->
        <ListBox Grid.Row="3" ItemsSource="{Binding Encounters}"
                 Background="Transparent" BorderThickness="0"
                 VirtualizingStackPanel.IsVirtualizing="True"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 Margin="8,0">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0,2"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <ContentPresenter/>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Style="{DynamicResource CardStyle}" Padding="10,8" Margin="4,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding VesselName}"
                                       FontWeight="SemiBold" FontSize="12"
                                       Foreground="{DynamicResource PrimaryTextBrush}"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding TimeText}"
                                       Style="{DynamicResource SubTextStyle}" FontSize="10"/>

                            <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal" Margin="0,2,0,0">
                                <TextBlock Text="{Binding CountryText}" FontSize="10"
                                           Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,8,0"/>
                                <TextBlock Text="{Binding TypeText}" FontSize="10"
                                           Foreground="{DynamicResource AccentBrush}"/>
                                <TextBlock Text="{Binding SpeedText}" FontSize="10" Margin="8,0,0,0"
                                           Foreground="{DynamicResource SecondaryTextBrush}"/>
                            </StackPanel>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding RatingText}"
                                       Foreground="#FFD700" FontSize="10"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
