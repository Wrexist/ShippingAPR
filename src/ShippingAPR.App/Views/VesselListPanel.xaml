<UserControl x:Class="ShippingAPR.App.Views.VesselListPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:ShippingAPR.App.Converters">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Padding="12,10"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,0,0,1">
            <Grid>
                <TextBlock Text="Vessels" Style="{DynamicResource HeaderTextStyle}"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <ComboBox SelectedItem="{Binding SortBy}" Width="80" FontSize="11"
                              Background="{DynamicResource TertiaryBackgroundBrush}"
                              Foreground="{DynamicResource PrimaryTextBrush}">
                        <ComboBoxItem Content="Name"/>
                        <ComboBoxItem Content="Speed"/>
                        <ComboBoxItem Content="ETA"/>
                        <ComboBoxItem Content="Distance"/>
                    </ComboBox>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Vessel List -->
        <ListBox Grid.Row="1"
                 ItemsSource="{Binding Vessels}"
                 SelectedItem="{Binding SelectedVessel, Mode=TwoWay}"
                 Background="Transparent"
                 BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 VirtualizingPanel.IsVirtualizing="True"
                 VirtualizingPanel.VirtualizationMode="Recycling">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Padding="10,8" Margin="2"
                            CornerRadius="4"
                            Background="{DynamicResource PrimaryBackgroundBrush}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Vessel type indicator -->
                            <Ellipse Grid.Column="0" Grid.RowSpan="2"
                                     Width="10" Height="10"
                                     Margin="0,0,8,0"
                                     VerticalAlignment="Center">
                                <Ellipse.Fill>
                                    <SolidColorBrush>
                                        <SolidColorBrush.Color>
                                            <Binding Path="Type">
                                                <Binding.Converter>
                                                    <converters:VesselTypeToColorConverter/>
                                                </Binding.Converter>
                                            </Binding>
                                        </SolidColorBrush.Color>
                                    </SolidColorBrush>
                                </Ellipse.Fill>
                            </Ellipse>

                            <!-- Name -->
                            <TextBlock Grid.Column="1" Grid.Row="0"
                                       Text="{Binding Name}"
                                       Foreground="{DynamicResource PrimaryTextBrush}"
                                       FontSize="13" FontWeight="SemiBold"
                                       TextTrimming="CharacterEllipsis"/>

                            <!-- Speed -->
                            <TextBlock Grid.Column="2" Grid.Row="0"
                                       Text="{Binding SpeedText}"
                                       Foreground="{DynamicResource SecondaryTextBrush}"
                                       FontSize="11" HorizontalAlignment="Right"/>

                            <!-- Destination + ETA -->
                            <TextBlock Grid.Column="1" Grid.Row="1"
                                       Text="{Binding Destination}"
                                       Foreground="{DynamicResource SecondaryTextBrush}"
                                       FontSize="11" TextTrimming="CharacterEllipsis"/>

                            <TextBlock Grid.Column="2" Grid.Row="1"
                                       Text="{Binding EtaText}"
                                       Foreground="{DynamicResource AccentBrush}"
                                       FontSize="11" FontWeight="SemiBold"
                                       HorizontalAlignment="Right"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
